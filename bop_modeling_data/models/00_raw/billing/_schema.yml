version: 2

models:
  - name: raw__screngn__xcd_bil_account
    description: "Raw model for billing account information from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_account'."
    columns:
      - name: bil_account_id
        description: 'Billing account identifier.'
      - name: bil_account_id_hash
        description: "Hashed billing account identifier. {{ doc('bil_account_id_hash') }}"
      # Add other relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe other relevant columns from the source table.'

  - name: raw__screngn__xcd_bil_act_summary
    description: "Raw model for billing account summary from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_act_summary'."
    columns:
      # Add relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe relevant columns from the source table.'

  - name: raw__screngn__xcd_bil_cash_dsp
    description: "Raw model for billing cash disbursements from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_cash_dsp'."
    columns:
      # Add relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe relevant columns from the source table.'

  - name: raw__screngn__xcd_bil_cash_receipt
    description: "Raw model for billing cash receipts from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_cash_receipt'."
    columns:
      # Add relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe relevant columns from the source table.'

  - name: raw__screngn__xcd_bil_des_reason
    description: "Raw model for billing designation reasons from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_des_reason'."
    columns:
      # Add relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe relevant columns from the source table.'

  - name: raw__screngn__xcd_bil_ist_schedule
    description: "Raw model for billing installment schedules from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_ist_schedule'."
    columns:
      # Add relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe relevant columns from the source table.'

  - name: raw__screngn__xcd_bil_pol_proc_req
    description: "Raw model for billing policy processing requests from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_pol_proc_req'."
    columns:
      # Add relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe relevant columns from the source table.'

  - name: raw__screngn__xcd_bil_policy_trm
    description: "Raw model for billing policy terms from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_policy_trm'."
    columns:
      # Add relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe relevant columns from the source table.'

  - name: raw__screngn__xcd_bil_policy
    description: "Raw model for billing policies from the 'screngn' source. It likely selects all columns from an external source table named 'xcd_bil_policy'."
    columns:
      # Add relevant column descriptions from the source table.
      - name: placeholder_column
        description: 'This is a placeholder. Describe relevant columns from the source table.'
