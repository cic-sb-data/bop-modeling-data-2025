version: 2

models:
  - name: chk__lkp__associated_policies__row_counts
    description: 'Check model to verify row counts at various stages of building `lkp__associated_policies`. It references multiple ephemeral models from the `07_tmp` directory.'
    columns:
      - name: cte
        description: 'Name of the common table expression (CTE) or step being checked.'
      - name: row_count
        description: 'Number of rows in the CTE.'
